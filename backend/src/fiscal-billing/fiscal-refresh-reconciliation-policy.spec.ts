import { fiscalRefreshReconciliationPolicy } from "./fiscal-refresh-reconciliation-policy";
const NOW=new Date("2026-08-24T12:00:00.456Z");describe("fiscal refresh reconciliation policy",()=>{it.each([[1,60],[2,300]] as const)("schedules temporary attempt %i after %i seconds",(attempt,seconds)=>expect(fiscalRefreshReconciliationPolicy(attempt,NOW,true)).toEqual({nextRefreshAt:new Date(NOW.getTime()+seconds*1000),automaticRecoveryExhausted:false}));it("exhausts attempt three and permanent failures",()=>{expect(fiscalRefreshReconciliationPolicy(3,NOW,true)).toEqual({nextRefreshAt:null,automaticRecoveryExhausted:true});expect(fiscalRefreshReconciliationPolicy(1,NOW,false)).toEqual({nextRefreshAt:null,automaticRecoveryExhausted:true});});it("uses the greater bounded Retry-After and preserves milliseconds",()=>expect(fiscalRefreshReconciliationPolicy(1,NOW,true,120)).toEqual({nextRefreshAt:new Date(NOW.getTime()+120_000),automaticRecoveryExhausted:false}));it.each([[0,NOW,true,null],[1,new Date(NaN),true,null],[1,NOW,true,86_401]] as const)("rejects invalid input",(a,d,t,r)=>expect(()=>fiscalRefreshReconciliationPolicy(a,d,t,r)).toThrow());});
