import{Injectable,Logger,OnModuleDestroy,OnModuleInit}from"@nestjs/common";import{randomUUID}from"node:crypto";import{JobDispatcherService}from"../../infrastructure/job-dispatcher";import{PLATFORM_QUEUE_KEYS}from"../../infrastructure/queue";import{PrismaService}from"../../prisma/prisma.service";import{FISCAL_REFRESH_RECONCILIATION_BATCH_SIZE,FISCAL_REFRESH_RECONCILIATION_JOB_NAME,FISCAL_REFRESH_RECONCILIATION_LEASE_MS,FISCAL_REFRESH_RECONCILIATION_POLL_MS,FISCAL_REFRESH_RECONCILIATION_VERSION,fiscalRefreshJobId}from"./fiscal-refresh-reconciliation.constants";import{logFiscalPollerFailure}from"./fiscal-poller-error-logging";
interface Claim{tenantId:string;billingDocumentId:string;refreshLockOwner:string;}@Injectable()export class FiscalRefreshReconciliationPublisher implements OnModuleInit,OnModuleDestroy{private logger=new Logger(FiscalRefreshReconciliationPublisher.name);private timer:ReturnType<typeof setTimeout>|null=null;private active:Promise<void>|null=null;private stopping=false;constructor(private prisma:PrismaService,private dispatcher:JobDispatcherService){}onModuleInit(){this.schedule(0);}async onModuleDestroy(){this.stopping=true;if(this.timer)clearTimeout(this.timer);this.timer=null;await this.active;}async publishDueRefreshes(){const claims=await this.claim();for(const row of claims)try{await this.dispatcher.dispatch({queueKey:PLATFORM_QUEUE_KEYS.FISCAL_REFRESH_RECONCILIATION,jobName:FISCAL_REFRESH_RECONCILIATION_JOB_NAME,payload:{tenantId:row.tenantId,billingDocumentId:row.billingDocumentId,refreshLockOwner:row.refreshLockOwner,eventVersion:FISCAL_REFRESH_RECONCILIATION_VERSION},metadata:{tenantId:row.tenantId},options:{jobId:fiscalRefreshJobId(row.refreshLockOwner),attempts:3,backoff:{type:"exponential",delay:2000},removeOnComplete:false,removeOnFail:false}});}catch{this.logger.error("Fiscal refresh reconciliation dispatch failed.");}}private schedule(ms:number){if(this.stopping)return;this.timer=setTimeout(()=>{this.timer=null;void this.cycle();},ms);}private async cycle(){if(this.stopping||this.active)return;const active=this.publishDueRefreshes().catch(error=>logFiscalPollerFailure(this.logger,"FiscalRefreshReconciliationPublisher",error));this.active=active;try{await active;}finally{this.active=null;this.schedule(FISCAL_REFRESH_RECONCILIATION_POLL_MS);}}private claim():Promise<Claim[]>{const now=new Date(),lease=new Date(now.getTime()+FISCAL_REFRESH_RECONCILIATION_LEASE_MS),owner=`frr-${randomUUID()}`;return this.prisma.$transaction(tx=>tx.$queryRaw<Claim[]>`WITH selected AS (SELECT "id","tenantId","providerNextRefreshAt" FROM "billing_documents" WHERE "lifecycleStatus"='SUBMITTED' AND "providerStatus"='PROCESSED' AND "taxAuthorityStatus"='PROCESSING' AND "providerReconciliationRequired" AND "issuedAt" IS NULL AND "providerNextRefreshAt" IS NOT NULL AND "providerNextRefreshAt"<=${now} AND "providerRefreshAttempts"<3 AND "providerDocumentId" IS NOT NULL AND "haciendaKey" IS NOT NULL AND "fiscalNumber" IS NOT NULL AND "providerEnvironment" IS NOT NULL AND "providerRequestHash" IS NOT NULL AND "providerLastAttemptAt" IS NOT NULL AND "submittedAt" IS NOT NULL AND "billingDocumentNumberSequenceId" IS NOT NULL AND "allocatedSequenceNumber" IS NOT NULL AND "issuanceIdempotencyKey" IS NOT NULL AND "fiscalEmissionAt" IS NOT NULL AND "fiscalIssueDate" IS NOT NULL AND "providerNextStatusCheckAt" IS NULL AND "providerStatusCheckLockOwner" IS NULL AND "providerStatusCheckLeaseUntil" IS NULL AND (("providerRefreshLockOwner" IS NULL AND "providerRefreshLeaseUntil" IS NULL) OR "providerRefreshLeaseUntil"<=${now}) ORDER BY "providerNextRefreshAt","id" LIMIT ${FISCAL_REFRESH_RECONCILIATION_BATCH_SIZE} FOR UPDATE SKIP LOCKED),eligible AS(SELECT selected.*,row_number()OVER(ORDER BY "providerNextRefreshAt","id")position FROM selected) UPDATE "billing_documents" d SET "providerRefreshLockOwner"=${owner}||'-'||eligible.position::text,"providerRefreshLeaseUntil"=${lease} FROM eligible WHERE d."id"=eligible."id" AND d."tenantId"=eligible."tenantId" RETURNING d."tenantId",d."id" AS "billingDocumentId",d."providerRefreshLockOwner" AS "refreshLockOwner"`);}}
